"""
Try running this script.
You will see that the script fails because of a syntax errorr.

Fix the synatx error and run again. Fix syntax errors until the script runs without an error.
"""

import random

random.seed(0)

# Create a random number
my_number = random.random[]

if my_number<0.5
  print("The number was less than 0.5)
else:
  print("The number was greater than 0.5")

